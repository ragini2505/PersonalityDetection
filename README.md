# Personality Detection from Resume

## How to Run
1. Clone the repo
2. Open PDFR.ipynb in Jupyter Notebook
3. Install required packages:
   pip install -r requirements.txt
4. Run the notebook step by step
